load("@rules_proto//proto:defs.bzl", "proto_library")
proto_library(
    name = "proto",
    # srcs = glob(["**/*.proto"]),
    srcs = [
        "ping/ping.proto",
    ],
    visibility = ["//visibility:public"],
)
